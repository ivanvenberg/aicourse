# Глава 3. Агенты — от чата к системе

**На созвоне (встреча 1 из 3):** `whattodoo.md` + `slides.html` — проект, челлендж агентов, MVP.  
**Этот файл** — теория для чтения дома. Один `project.md` на встречи 4–6.

**После уроков 1–2** ты знаешь, как устроена модель и API. **После урока 3** — как говорить с ней точнее и хранить данные.

**Урок 4** — переход к **агентам**: AI, который не только отвечает, а **делает** — читает файлы, запускает код, ходит в сервисы, ищет в интернете.

*Читай по порядку. В конце — «Проверь себя».*

---

## Проверь себя

| Вопрос | ✅ / ❌ |
|--------|--------|
| Чем чат, субагент и агент с инструментами отличаются | |
| Что такое ReAct | |
| Что такое tool use / function calling | |
| Что такое MCP и зачем он агенту | |
| Что такое скилл в Claude Code | |
| Зачем CLAUDE.md, MEMORY.md, .env | |
| Когда нужен Perplexity / Parallel, а не «спроси модель» | |
| Три ловушки агентов (дети, кексы, мусор) | |

---

## 3.1 Четыре уровня работы с AI

```
Уровень 1 — Чат          Спросил → ответил. Файлы копируешь сам.
Уровень 2 — Редактор     Агент правит файлы в проекте (Cursor).
Уровень 3 — Субагент     «Сходи, сделай X, верни результат».
Уровень 4 — Агент+MCP    Руки в Notion, Sheets, браузере, поиске.
```

**AI-native** — ты живёшь на уровнях 2–4: задача словами, результат в файлах/сервисах, ты проверяешь.

**Agent Teams** (несколько сессий Claude Code, которые пишут друг другу) — эксперимент для терминала. **Не обязательны.** На курсе достаточно субагентов и MCP.

---

## 3.2 Агент: цикл «думаю → делаю → смотрю»

**Чат:**
```
Ты → вопрос → модель → текст
```

**Агент (ReAct — Reason + Act):**
```
Ты → задача
  → «надо прочитать project.md»   → читает файл
  → «тут нет API ключа»           → просит .env
  → «запущу скрипт»               → терминал
  → «тест упал»                   → правит код
  → «готово»                      → отчёт
```

Каждый шаг опирается на **реальный** результат прошлого шага, не на фантазию.

**Пример промпта:**
```
Прочитай data/reviews.json.
Напиши Python-скрипт для подсчёта средней оценки.
Запусти. Если ошибка — исправь и запусти снова.
Сохрани результат в results/avg.md
```

---

## 3.3 Субагенты

**Субагент** — отдельный запуск агента на подзадачу. Результат возвращается **тебе** (или главному агенту), не другим субагентам.

**Когда использовать:**
- три независимых анализа (без спора между ними);
- параллельно: «один читает код, второй пишет тесты, третий README».

**Пример:**
```
Запусти 3 субагента параллельно:
1. Найди все места где вызывается API
2. Найди уязвимости (ключи в коде, нет .env)
3. Предложи 3 улучшения архитектуры
Каждый → отдельный файл в audit/
Потом сведи в audit/summary.md
```

**Не путать с театром в одном чате** («сыграй генератора и критика») — это одна голова, не субагенты.

---

## 3.4 Tool use (function calling)

Модель может не только писать текст, но и **вызывать функции**:

```
Пользователь: «Какая погода в Москве?»

Модель решает: нужен tool weather(city)
→ вызывается реальная функция
→ результат: -2°C, снег
→ модель: «В Москве -2°C, снег»
```

В Claude Code tools встроены: Read, Write, Bash, Grep…  
В MCP — tools внешних сервисов.

**Ты проектируешь агента**, когда решаешь: какие tools ему доступны и какие **запрещены**.

---

## 3.5 MCP — «руки» агента

**MCP (Model Context Protocol)** — стандарт: один раз подключил сервис → агент умеет им пользоваться.

| Без MCP | С MCP |
|---------|--------|
| Только файлы в папке | Notion, Figma, Google Sheets, Gmail… |
| Ты копируешь из сервиса | Агент читает/пишет сам |

**Установка (в папке проекта):**
```bash
claude mcp add notion
claude mcp add gsheets
claude mcp add figma
```

Токен — в настройках сервиса, не в чате.

**Пример задачи:**
```
Через Notion MCP: прочитай страницу «Интервью 12.03»,
выдели 5 цитат, сохрани в insights.md
```

**Parallel MCP** (поиск в интернете для агентов) — см. §3.8.

---

## 3.6 Скиллы

**Скилл** — сохранённая инструкция, вызов одной командой `/имя`.

Путь: `.claude/skills/имя/skill.md`

```markdown
---
name: weekly-report
description: Собирает недельный отчёт из папки data/
---

1. Прочитай все CSV в data/
2. Запусти scripts/summary.py
3. Сохрани отчёт в reports/YYYY-MM-DD.md
4. Формат: цифры крупно, три вывода
```

Вызов: `/weekly-report`

**Скилл = твой рабочий процесс в тексте.** Чем больше повторяешь — тем больше скиллов.

---

## 3.7 Память проекта

| Файл | Что писать |
|------|------------|
| `CLAUDE.md` | Стек, стиль кода, запреты («не удаляй файлы») |
| `MEMORY.md` | Кто ты, проект, аудитория, тон |
| `AGENTS.md` | Права: MAY / MAY NOT |
| `.env` | API-ключи — **никогда** в git и не в промпте |

**Пример CLAUDE.md:**
```markdown
## Стек
HTML + JS, Supabase, OpenRouter

## Правила
- Коммит перед большой правкой
- Секреты только в .env
- Не удалять — переносить в /archive/
```

Агент читает это при старте — не объясняешь контекст заново.

---

## 3.8 Поиск в интернете: Perplexity и Parallel

Модель **не знает** свежие новости и **выдумывает** ссылки. Для фактов из сети — **поисковый API**.

📖 **Полная глава (все API Parallel, живые примеры, скрипты):** [`docs/research-apis.md`](../docs/research-apis.md)  
▶️ **Запуск:** [`examples/research/`](../examples/research/)

### Perplexity API

**Зачем:** готовый **синтез** ответа + массив `citations` (URL).

**Когда:** «что такое MCP», «что изменилось в API», быстрый ресёрч на 30 секунд.

**Паттерн:**
```
1. POST /chat/completions (model: sonar)
2. choices[0].message.content + citations[]
3. Сохранить в research.md (текст + список URL)
4. Агент пишет финал только с опорой на research.md
```

**Реальный фрагмент ответа** (проверено на API курса):
```json
{
  "choices": [{"message": {"content": "MCP — open-source стандарт…"}}],
  "citations": [
    "https://www.anthropic.com/news/model-context-protocol",
    "https://en.wikipedia.org/wiki/Model_Context_Protocol"
  ],
  "usage": {"cost": {"total_cost": 0.00512}}
}
```

Скрипт: `examples/research/ask_perplexity.py`. Ключ: `PERPLEXITY_API_KEY` в `.env`.

### Parallel API — семейство, не один endpoint

| API | Что делает | Латентность |
|-----|------------|-------------|
| **Search** | objective + keywords → `results[].excerpts` | секунды |
| **Extract** | URL → текст страницы | секунды |
| **Task** | ресёрч + `output.basis[]` (citations, confidence) | сек–часы |
| **Entity Search** | люди/компании | секунды |
| **FindAll / Monitor** | датасеты, вебхуки при изменениях | фон |

**Search — реальный ответ** (сокращённо):
```json
{
  "search_id": "search_2fe33c3f6303405997afdf1f673de06d",
  "results": [{
    "url": "https://encore.dev/blog/you-probably-dont-need-a-vector-database",
    "title": "pgvector Guide: Vector Search and RAG in PostgreSQL",
    "excerpts": ["pgvector is a PostgreSQL extension that adds vector storage..."]
  }]
}
```

**Task — реальный ответ** (`processor: lite`, ~40 с):
```json
{
  "output": {
    "type": "json",
    "content": {"what_is": "Claude Code is Anthropic's terminal-based...", "key_features": ["..."]},
    "basis": [{
      "field": "what_is",
      "confidence": "high",
      "citations": [{"url": "https://code.claude.com/docs/en/overview", "title": "..."}]
    }]
  }
}
```

Скрипты: `parallel_search.py`, `parallel_task.py`, `parallel_extract.py`, `research_router.py`.

**В Claude Code:** Parallel MCP (`parallel-web`) — агент вызывает Search без скрипта.

### Когда что

| Задача | Инструмент |
|--------|------------|
| Быстрый ответ-эссе с ссылками | Perplexity |
| Выдержки 5–20 страниц для RAG | Parallel Search |
| Известный URL → текст | Parallel Extract |
| JSON-поля по компании/теме | Parallel Task |
| Код в твоём проекте | Агент + файлы |
| Точные цифры по твоим данным | Python-скрипт (урок 3) |

---

## 3.9 Декомпозиция задач

Большая задача в один промпт → агент теряет половину.

**Плохо:**
```
Сделай полноценный SaaS с оплатой и админкой
```

**Хорошо — граф:**
```
1. Схема БД (schema.md)
2. API endpoints (api.md)
3. Одна страница UI
4. Подключить .env
5. Деплой
```

Один шаг → проверка → следующий. **AI-native** = ты держишь архитектуру, агент крутит шаги.

---

## 3.10 Три ловушки агентов

| Ловушка | Что делает | Защита |
|---------|------------|--------|
| **Дети** | Отключает тесты вместо починки | «Сколько тестов было/стало?» |
| **Кексы** | Хардкод под тест | Читай код, не только «passed» |
| **Мусор** | debug-логи, `temp2` | «Убери мусор после задачи» |

**Всегда:** `git commit` перед тем как отпустить агента на большую правку.

---

## 3.11 Plan-and-Execute vs ReAct

| | ReAct | Plan-and-Execute |
|--|-------|------------------|
| Как | Шаг за шагом, видит результат | Сначала весь план, потом выполнение |
| Когда | Неясно что всплывёт по ходу | Структура задачи понятна |
| Риск | Блуждание | План устарел — не гибкий |

**Пример Plan-and-Execute:**
```
1. Составь план из 5 шагов для подключения Notion MCP
2. Покажи план — жди моего ок
3. Выполняй по одному шагу
```

**Пример ReAct:**
```
Подключи Notion MCP. Если ошибка — разберись и продолжай.
```

---

## 3.12 Cursor: Rules и контекст

| Файл | Где | Зачем |
|------|-----|--------|
| `.cursor/rules/*.mdc` | Cursor | Правила для агента в Cursor |
| `CLAUDE.md` | Claude Code | То же для терминала `claude` |
| `MEMORY.md` | Проект | Кто ты, проект, тон |

**Пример rule:**
```markdown
---
description: Стиль JS в проекте
---
- async/await, не .then()
- fetch с обработкой ошибок
- комментарии только для неочевидного
```

Правила = системный промпт, который **не нужно повторять** каждый чат.

---

## 3.13 MCP: полный список идеей

| MCP | Профессия | Пример задачи |
|-----|-----------|---------------|
| `notion` | UX, главред | Транскрипт → страница в Notion |
| `gsheets` | СМО, CRM | Прочитать метрики → отчёт |
| `figma` | Дизайн | Описать макет → бриф |
| `gdrive` | Редактор | Черновик в Google Doc |
| `gmail` | CRM | Черновик письма (не отправлять без ревью!) |
| `parallel` | Все | Поиск в сети для агента |

**Установка:**
```bash
claude mcp add notion
# следуй инструкциям в терминале — токен OAuth
```

**Проверка:** спроси агента «какие MCP подключены?»

---

## 3.14 Perplexity API — подробнее

См. также §3.8 и [`docs/research-apis.md`](../docs/research-apis.md) §3.

**Модели:** `sonar` (быстро), `sonar-pro` (глубже). Список — в docs.perplexity.ai.

**Поля ответа, которые должен знать инженер:**

| Поле | Зачем |
|------|--------|
| `choices[0].message.content` | Текст для пользователя |
| `citations[]` | URL для проверки |
| `usage.cost.total_cost` | Бюджет демо/бота |
| `search_results[]` | (если есть) сниппеты до синтеза |

**Антипаттерн:** скопировать ответ в промпт без citations → через неделю не вспомнишь источник.

**Промпт для агента:**
```
Вызови ask_perplexity.py с вопросом «…».
Сохрани ответ и citations в research/YYYY-MM-DD-topic.md.
Напиши вывод в 5 буллетах — каждый с [источник](url).
```

---

## 3.15 Parallel API — подробнее

См. [`docs/research-apis.md`](../docs/research-apis.md) §4–7.

### Search
- Вход: `objective` + `search_queries` (2–3 keyword — лучше recall).
- Выход: `search_id`, `results[]` → `url`, `title`, `publish_date`, `excerpts[]`.
- **Grounding:** «Ответь только по excerpts в sources.md».

### Extract
- Вход: `urls[]`, опционально `objective`.
- Endpoint: `POST /v1beta/extract`.
- Когда: ссылка уже есть (из Search или от пользователя).

### Task
- `POST /v1/tasks/runs` → `run_id`, `status: queued`.
- Poll: `GET /v1/tasks/runs/{run_id}/result` до `completed`.
- **Processors:** `lite` (курс, секунды), `base`/`core`, `pro`/`ultra` (минуты–часы, webhook).
- **basis[]** — золото: `field`, `citations`, `reasoning`, `confidence`.

**Кейс:** CSV компаний → Task с `output_schema` JSON → `merged.csv`.

### Parallel в Claude Code
```bash
/plugin marketplace add parallel-web/parallel-agent-skills
/parallel:setup
```

### Комбо (домашка)
`research_router.py` — один вопрос → Perplexity + Parallel Search в один `.md`.

---

## 3.16 Параллельные вызовы

**В коде** — несколько API одновременно:
```python
import asyncio
# параллельно: Perplexity + свой RAG + скрипт
# собрать в один ответ
```

**Субагенты** — параллельно в Cursor/Claude Code без asyncio.

**Правило:** параллель только для **независимых** задач. Одна БД — последовательно.

---

## 3.17 Права и песочница

Ограничивай агента явно в `AGENTS.md`:

```markdown
## MAY
- Читать /src, /docs
- Запускать npm test

## MAY NOT
- git push
- rm -rf
- Отправлять email без подтверждения
- Менять .env
```

Меньше прав — меньше сюрпризов.

---

## 3.18 Пайплайны (цепочки)

```
Триггер → Шаг 1 → Шаг 2 → Шаг 3 → результат
```

**Пример главред:**
```
1. /find-news     — Parallel/Perplexity → news.md
2. /draft         — черновик из news.md
3. Ты             — правка 5 минут
4. Публикация
```

Скиллы = звенья пайплайна.

---

## 3.19 Контекст агента

Длинный чат → модель «забывает» начало → режет углы.

**Три приёма:**
1. **Новый чат** на новую задачу  
2. **Файлы-сводки** (`context-summary.md`)  
3. **CLAUDE.md** — правила не обсуждаются каждый раз  

```
Суммаризируй сессию: что сделали, решения, что осталось.
Сохрани в context-summary.md
```

---

## Итого главы 3

| Концепт | Суть |
|---------|------|
| AI-native | Агент + файлы + tools, ты ревьюишь |
| ReAct | Думай → действуй → смотри результат |
| Субагент | Подзадача, отчёт тебе |
| MCP | Руки в сервисах |
| Скилл | `/команда` = процесс |
| Perplexity / Parallel | Факты из сети с источниками |
| .env | Ключи не в git |
| Декомпозиция | Маленькие шаги с проверкой |

**Следующий урок:** RAG в проде, модели, боты, безопасность, мультимодальность.
