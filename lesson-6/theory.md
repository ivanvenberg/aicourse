# Глава 5. AI-native профессионал

**Урок 6** — как **жить** с AI-кодом: не сломаться, не потерять контроль, не переплатить. Плюс карта инструментов и куда расти.

---

## Проверь себя

| Вопрос | ✅ / ❌ |
|--------|--------|
| Зачем коммитить перед задачей агенту | |
| Семантические коммиты | |
| Как ревьюить код, который писал AI | |
| Почему техдолг растёт быстрее с вайбкодингом | |
| Как считать стоимость AI-продукта | |
| Что такое human-in-the-loop | |
| Что остаётся за человеком, что автоматизируется | |
| Полная карта стека AI-native | |

---

## 5.1 Что значит AI-native

Не «знаю ChatGPT». А:

```
✓ Формулирую задачи под агента (образ целиком, не пиксели)
✓ Декомпозирую — маленькие шаги с проверкой
✓ Знаю, когда скрипт, когда RAG, когда Perplexity/Parallel
✓ Держу секреты в .env, данные в БД
✓ Ревьюю diff — принял код = мой код
✓ Измеряю: токены, время, качество (golden tests)
✓ Собираю скиллы и MCP под свою профессию
```

---

## 5.2 Git — страховка от агента

AI **удалит** файл без предупреждения. Git — машина времени.

```bash
git add .
git commit -m "chore: перед рефактором auth"
# ... агент работает ...
# сломалось:
git checkout -- .
# или
git reset --hard HEAD
```

### Семантические коммиты

```
feat: добавить RAG по knowledge.md
fix: бот не отвечал на пустое сообщение
refactor: вынести API в api.js
docs: обновить README
chore: обновить зависимости
```

История читается как дневник.

### Ветки для экспериментов

```bash
git checkout -b experiment/new-ui
# агент крутит...
# понравилось → merge
# нет → checkout main && branch -D experiment/new-ui
```

---

## 5.3 Code review AI-кода

Ты **не обязан** читать каждую строку. Обязан **понимать**, что принимаешь.

**Четыре приёма:**

1. **Объясни как пятилетнему**
```
Объясни этот файл простыми словами. Что может сломаться?
```

2. **Злой ревьюер**
```
Притворись строгим ревьюером. Найди баги и дыры безопасности.
```

3. **Чеклист**
```
□ .env не в коде
□ ошибки обработаны
□ пустой ввод
□ нет debug-логов
```

4. **Diff перед accept**
В Cursor смотри, что **удалено**. Много красного — стоп, спроси зачем.

---

## 5.4 Технический долг

Вайбкодинг **быстрый** → долг копится **быстро**:
- дубли функций
- `interim_result_5`
- закомментированный код
- «временно» без TODO

**После каждой крупной фичи:**
```
Найди дубли, мусор, функции > 30 строк. Упрости, не ломая поведение.
```

На дедлайне — только критичное. Остальное в `TECH-DEBT.md`.

---

## 5.5 Когда перестать доверять агенту

| Сигнал | Действие |
|--------|----------|
| Не понимаешь diff | Попроси объяснить или упрости задачу |
| Одна ошибка 5 раз | Новый чат, другая формулировка |
| Ломается несвязанное | Нарисуй схему на бумаге, потом агенту |
| Auth, деньги, PII | Каждую строку сам |

**Human-in-the-loop** — AI черновик, **ты** финальное решение на важном.

---

## 5.6 Стоимость AI-продукта

```
Счёт ≈ (входные токены + выходные токены) × цена модели × число пользователей
```

**Как экономить:**
- Flash для рутины, Sonnet для сложного
- Короткий системный промпт
- RAG top-3, не top-20
- Кэш частых ответов
- Лимит запросов на пользователя

**Пример:** бот 1000 сообщений/день × 500 токенов × Flash — посчитай в Excel **до** запуска.

---

## 5.7 Полная карта стека AI-native

```
┌─────────────────────────────────────────────────────────┐
│  ТЫ: задача, ревью, этика, продуктовые решения          │
└──────────────────────────┬──────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────┐
│  РЕДАКТОР: Cursor + агент (файлы, терминал, git)       │
│  Скиллы · CLAUDE.md · MEMORY.md                        │
└──────────────────────────┬──────────────────────────────┘
                           │
        ┌──────────────────┼──────────────────┐
        ▼                  ▼                  ▼
   ┌─────────┐       ┌───────────┐      ┌────────────┐
   │  MCP    │       │ Субагенты │      │  Скрипты   │
   │ Notion  │       │ параллель │      │  Python    │
   │ Sheets  │       │ задачи    │      │  точные    │
   │ Figma   │       └───────────┘      │  цифры     │
   └─────────┘                          └────────────┘
        │
        ▼
┌───────────────────────────────────────────────────────┐
│  ВНЕШНИЕ API                                          │
│  OpenRouter (модели) · Perplexity (факты+ссылки)      │
│  Parallel (поиск/обогащение) · Image API              │
└───────────────────────────┬───────────────────────────┘
                            │
        ┌───────────────────┼───────────────────┐
        ▼                   ▼                   ▼
   ┌─────────┐        ┌──────────┐       ┌───────────┐
   │ Supabase│        │ Telegram │       │  Vercel   │
   │ БД+RAG  │        │  бот     │       │  деплой   │
   └─────────┘        └──────────┘       └───────────┘
```

**Уроки 1–3:** низ (модель, промпты, БД, скрипты).  
**Урок 4:** середина (агент, MCP, поиск).  
**Урок 5:** RAG, бот, API, безопасность.  
**Урок 6:** ты сверху (git, ревью, деньги, процесс).

---

## 5.8 Что автоматизируется, что нет

| Автоматизируется | Остаётся за тобой |
|------------------|-------------------|
| Шаблонный код | Зачем это нужно людям |
| Тесты, доки | Верно ли для **твоего** случая |
| Рутина в Sheets/Notion | Этика, ответственность |
| Первый черновик | Финал для клиента |
| Поиск по докам (RAG) | Какие доки класть в базу |

**Ошибся бот — твой бот.** AI не несёт ответственности.

---

## 5.9 Деплой и домен (коротко)

```
git push → GitHub → Vercel → https://....vercel.app
```

**Домен:** купил → DNS-записи от Vercel → `https://твой.ру`

Продукт AI-native = **ссылка работает**, ключи на сервере в env vars (не в репо).

---

## 5.10 PROMPTS.md — версионирование промптов

Храни системные промпты в git, не только в коде:

```markdown
# PROMPTS.md

## bot-support v3 (2026-06-01)
- Добавлено: «не знаю»
- Убрано: длинный список FAQ в промпте → RAG

## bot-support v2
...
```

Меняешь промпт → коммит → видно что сломалось в golden tests.

---

## 5.11 CI/CD с AI-кодом

**Минимальный pipeline (GitHub Actions):**
```yaml
# на каждый push:
# 1. npm test / pytest
# 2. lint (опционально)
# 3. deploy preview на Vercel
```

AI ломает тесты часто — **тесты в CI** ловят до продакшна.

Промпт агенту: «Добавь GitHub Action: pytest на push».

---

## 5.12 Инциденты: бот пошёл не туда

1. **Выключи** бота (stop webhook / env `MAINTENANCE=1`)
2. Смотри логи — что спросили, что ответил
3. Фикс промпта или RAG
4. Прогон `tests/ai-golden.md`
5. Включи снова

Запиши в `INCIDENTS.md` — учишься на ошибках.

---

## 5.13 Приватность и данные

- Не отправляй в API **паспорта, пароли, полные базы клиентов**
- Проверь ToS провайдера (обучают ли на данных)
- EU/GDPR: право на удаление → уметь удалить user data из Supabase
- Анонимизируй логи

**AI-native** = удобство **и** границы.

---

## 5.14 Работа в команде

| Практика | Зачем |
|----------|--------|
| Общий `CLAUDE.md` | Один стиль кода |
| PR + ревью | Даже AI-код через глаза коллеги |
| `TECH-DEBT.md` | Общий список «потом починим» |
| Не коммитить `.env` | Все |

---

## 5.15 Personal OS / Second Brain (концепт)

Не отдельный продукт курса — **паттерн:**

```
MEMORY.md + скиллы + MCP + RAG по твоим файлам
= агент знает твой контекст годами
```

Папки: `notes/`, `projects/`, `prompts/` — агент ищет и действует.

---

## 5.16 Глоссарий AI-native

| Термин | Одной фразой |
|--------|--------------|
| Токен | Кусок текста, единица счёта API |
| RAG | Поиск в твоих данных → в промпт |
| MCP | Стандарт «руки» агента в сервисах |
| Скилл | Сохранённый workflow `/команда` |
| ReAct | Думай → действуй → смотри |
| Embedding | Текст → вектор для поиска |
| Injection | Взлом через пользовательский ввод |
| Golden test | Эталонные вопросы/ответы |
| Webhook | Сервер сам получает события |
| Human-in-the-loop | AI черновик, человек финал |

---

## 5.17 Куда расти после курса

| Направление | Инструменты |
|-------------|-------------|
| Глубже агенты | MCP, больше скиллов, Parallel Task |
| Глубже RAG | pgvector, reranking, eval pipelines |
| Глубже эксплуатация | мониторинг, логи, A/B промптов |
| Своя профессия | 5 скиллов под рутину отрасли |

**Не гонись за всем.** Один стек, доведённый до автоматизации рутины — уже AI-native.

---

## 5.18 Чеклист «я AI-native» (самопроверка)

- [ ] Могу задеплоить продукт с AI внутри
- [ ] RAG или knowledge с «не знаю»
- [ ] Ключи в .env, .gitignore настроен
- [ ] 3+ скилла или MCP под работу
- [ ] Знаю цену своего API в месяц (хотя бы порядок)
- [ ] Golden tests на AI-ответы
- [ ] git commit перед большой правкой агента
- [ ] Использовал Perplexity или Parallel для фактов из сети
- [ ] Могу объяснить свой код простыми словами

---

## 5.19 Демо-день — структура 5 минут

Не питч инвесторам. **Показ работающего инструмента.**

| Минута | Что говоришь / делаешь |
|--------|-------------------------|
| 0:00–0:30 | Кому и какая рутина (из `project.md`) |
| 0:30–3:30 | **Живое демо** — лендинг → фича → результат на глазах |
| 3:30–4:30 | Стек в одном предложении: модель, RAG/поиск, деплой |
| 4:30–5:00 | Одна цитата из `feedback.md` + что сделаешь дальше |

**Запрещено:** слайды вместо клика, «мы используем AI», чтение кода построчно.

**Если сломалось:** покажи скринкаст 30 сек + честно «вот что чиню».

---

## Итого главы 5

| Концепт | Суть |
|---------|------|
| AI-native | Агент + tools + ревью + измерение |
| Git | Страховка и история |
| Code review | Принял = твоё |
| Техдолг | Чисти регулярно |
| Стоимость | Токены × пользователи |
| Human-in-the-loop | AI черновик, ты финал |
| Карта стека | Редактор → MCP → API → БД/бот/деплой |

Шесть уроков — фундамент. Дальше — углубление в **свою** профессию, не новые buzzwords.
